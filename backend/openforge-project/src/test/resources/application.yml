spring:
  datasource:
    url: jdbc:h2:mem:projecttest;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
    username: sa
    password: ""
  flyway:
    table: flyway_project_history

openforge:
  security:
    auth-base-url: http://localhost:0
    internal-token: test-internal-token
