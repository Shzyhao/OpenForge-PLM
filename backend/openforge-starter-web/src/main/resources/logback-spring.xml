<?xml version="1.0" encoding="UTF-8"?>
<!--
  OpenForge 共享日志格式（B3 可观测）：随 starter-web 分发，服务自有 logback-spring.xml 优先生效。
  traceId 来自网关 X-Trace-Id（TraceIdFilter → MDC），跨服务串联一次请求的全部日志。
-->
<configuration>
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] [%X{traceId:-}] %logger{36} - %msg%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>
</configuration>
