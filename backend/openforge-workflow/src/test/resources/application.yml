spring:
  datasource:
    url: jdbc:h2:mem:workflowtest;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
    driver-class-name: org.h2.Driver
    username: sa
    password: ""
  flyway:
    table: flyway_workflow_history

openforge:
  security:
    auth-base-url: http://localhost:0   # 测试中 PermissionQueryClient 被 MockBean 替换
    internal-token: test-internal-token
