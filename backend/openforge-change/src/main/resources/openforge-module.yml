# 业务模块：变更管理（依赖流程引擎）
moduleKey: change
moduleType: BUSINESS
displayName: 变更
version: 0.1.0
routes:
  - /api/v1/changes
dependencies: [workflow]
menu:
  - { path: /change, title: 变更, icon: SwapOutlined }
flyway:
  historyTable: flyway_change_history
