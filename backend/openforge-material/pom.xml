<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.openforge</groupId>
        <artifactId>openforge-backend</artifactId>
        <version>0.1.0-SNAPSHOT</version>
    </parent>

    <artifactId>openforge-material</artifactId>
    <name>OpenForge Material</name>
    <description>物料与 BOM 服务（M2-1：物料分类树 + 物料主数据 CRUD + 编号引擎取号）</description>

    <dependencies>
        <dependency>
            <groupId>com.openforge</groupId>
            <artifactId>openforge-starter-data</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>com.openforge</groupId>
            <artifactId>openforge-starter-security</artifactId>
            <version>${project.version}</version>
        </dependency>
        <!-- Nacos 服务发现（F1 尾）：默认关闭，部署时 NACOS_ENABLED=true 开启 -->
        <dependency>
            <groupId>com.alibaba.cloud</groupId>
            <artifactId>spring-cloud-starter-alibaba-nacos-discovery</artifactId>
        </dependency>
        <!-- B1 配置中心：默认关闭（NACOS_CONFIG_ENABLED），optional import 断连安全 -->
        <dependency>
            <groupId>com.alibaba.cloud</groupId>
            <artifactId>spring-cloud-starter-alibaba-nacos-config</artifactId>
        </dependency>


        <!-- OpenAPI 文档: /v3/api-docs + /swagger-ui.html（直连服务端口访问） -->
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
        </dependency>


        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <scope>provided</scope>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <!-- exec classifier：主 artifact 保持普通 jar（openforge-mono 依赖其类与资源）；可执行 fat jar 为 -exec.jar -->
                    <classifier>exec</classifier>
                </configuration>
                <executions>
                    <execution>
                        <goals>
                            <goal>repackage</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
