# OpenForge 框架化路线

> 从应用到框架的差距分析与实施计划
> 版本：v1.3 ｜ 状态：F1~F3 完成 + B3/B4/C/D-3 尾项交付（B2 事件总线/配置中心待做）｜ 更新日期：2026-08-28
> 前提：v1.1.0 应用主体已完成（8 个 Java 微服务 + Python AI 网关 + React 前端，32 个 PR）

---

## 1. 定位

OpenForge 当前是一套**业务写死的 PLM 应用**。框架化 = 变成**可让他人快速构建自己系统的平台底座**：
二次开发者不写（或少写）后端代码即可定义新业务对象、界面与流程，并自动获得权限、编号、审计、AI 等平台能力。
架构文档（元数据驱动 / 低代码平台 / 动态对象运行时）即为此设计，实现进度约三分之一。

## 2. 差距清单（六域）

### A. 平台内核化（框架的本质，最大差距）

| # | 事项 | 现状 | 目标 |
|---|------|------|------|
| A1 | 动态对象运行时 | ✅ F2 交付（#37/#38） | 对象建模器 + `/objects/{objectKey}/records` 通用 CRUD——零后端代码定义新对象 |
| A2 | 建模→DDL→AI 闭环 | ✅ F2 交付（#40）：发布=DDL+权限点+知识同步+AI 表登记 | 建模发布自动生成 Flyway 迁移 + Schema 知识同步（新对象即刻可被 AI 查询） |
| A3 | 表单/列表设计器 | ✅ F3 交付（#49）：布局制品 + 界面设计器页 + 数据页应用 | 至少补表单 + 列表两个设计器 |
| A4 | 模块化注册 | ✅ 交付（#46/#43/#44 + A4-4）：部署即注册/停用即摘除/启动自检/依赖守护，动态对象 EXTENSION 同构 | 模块启停机制（不装某模块则不路由不迁移），内核/业务边界——设计见《OpenForge-F2模块注册机制设计》 |
| A5 | Starter 化 | ✅ F3 交付（#50）：三件套 + 8 服务 pom 收敛 + 中央 MP 装配 | `openforge-starter-web/data/security`：引依赖即得统一响应/权限/审计/编号/多租户 |

### B. 基础设施（当前为单机演示级）

| # | 事项 | 现状 |
|---|------|------|
| B1 | 服务注册发现 + 配置中心 | ✅ 服务发现 #45 + 配置中心 #74（均默认关闭，optional import 断连安全，远程覆盖本地；dev NACOS=1 一键启用。回路测试挂 NACOS_LOOP_TEST 门：Testcontainers nacos 容器持久化怪癖待专项排查，默认关闭语义已全量实证） |
| B2 | 事件总线 | 设计定稿（《OpenForge-B2事件总线设计》：信封/一域一topic/幂等/outbox 分期，默认关闭回退同步 HTTP）；实施 B2-1~B2-3 待做 |
| B3 | 可观测 | ✅ #54：Prometheus 指标（九服务+网关）+ TraceId 全链路贯穿 + 共享日志格式 + monitoring compose 栈（Grafana JVM/网关/事件总线看板模板已内置 #76；SkyWalking/分布式追踪随规模引入） |
| B4 | 部署形态 | ✅ #56：Dockerfile 三件 + 生产 compose 全栈（密钥防默认上线/自动路由）+ Helm chart 骨架（YAML 校验过，helm lint 留待部署环境） |

### C. 多租户（SaaS 前提）

✅ F3 交付（#48）：JWT 租户声明 + 网关 X-User-Tenant 转发 + SQL 自动注入过滤（TenantLineInnerInterceptor + 动态表显式过滤）+ 租户主档/用户归属管理；单租户部署（默认 0）行为不变。文件/向量按租户隔离随对象存储/向量库落地补齐。

### D. 开发者体验

1. **API 文档**（OpenAPI/Swagger）——✅ F1 交付；
2. 脚手架生成器 `openforge-cli new-service`——✅ F3 交付（#51，selftest + 真实编译验证）；
3. 二开文档——✅ #58《OpenForge-二开指南》（三种扩展路径/惯例/AI/部署/排查）；组件规范与插件指南随生态演进；
4. 版本化迁移 + 升级指南。

### E. 质量保障

**Testcontainers 集成测试矩阵**——✅ F1 交付（auth/metadata 真实 PG 冒烟固化进 CI，三轮真实冒烟抓到的 7 个 H2 盲区缺陷已机制性兜底）。

### F. 产品化与生态

安装初始化向导、行业模板包（离散/汽配/电子）、i18n、文档站 + 在线 Demo、开源治理（内核 Apache-2.0 + 高级模块商业化）。

## 3. 实施路线

| 阶段 | 内容 | 定位 | 状态 |
|------|------|------|------|
| **F1 基础达标** | OpenAPI 文档（springdoc 全服务）+ Testcontainers 测试矩阵（真实 PG 跑 Flyway 全量与核心链路）+ Nacos 服务发现 | 从"能跑的 Demo"到"可信的底座" | ✅ 完成（#33/#34/#45：Nacos 默认关闭随部署启用，注册表协议不变演进） |
| **F2 核心卖点** | 动态对象运行时 + 建模→DDL→AI 闭环 + 模块注册机制 | 兑现"低代码 × AI"架构承诺 | ✅ 完成（#37~#41 动态对象四刀 + #46/#43/#44 模块注册机制 + A4-4 EXTENSION 同构） |
| **F3 商业化** | 多租户 + 表单设计器 + 脚手架 CLI + Starter 化 | 可对外交付的框架产品 | ✅ 完成（#48~#51，2026-08-27；Helm 随部署环境另行交付） |

## 4. 工程备忘（已踩坑沉淀）

1. **网关路由清单**：新增 API 前缀必须同步 gateway `application.yml`（三次被真实冒烟捕获）；F2 实现启动自检（扫描各服务 Controller 前缀与路由比对，方案 C6 同源思路）；
2. **H2 与 PG 的 SQL 方言**：跨库迁移避免 `ON CONFLICT`（H2 不支持），用 `WHERE NOT EXISTS` 幂等写法；
3. **MyBatis-Plus 三坑**：分页拦截器必须注册（selectPage 不算 total）；`updateById` 默认忽略 null（置空列需 `FieldStrategy.ALWAYS`）；`@Value` 不绑定 yml 列表（用逗号分隔字符串）；
4. **测试矩阵盲区**：纯 Mockito 单测无法覆盖 Spring 容器装配、H2 独立库无法覆盖多服务共库——Testcontainers 为唯一可信层。
